name: Deploy TFT Crawler to Oracle Cloud

on:
  push:
    branches: [ TwoPhase ] # Trigger on every commit to TwoPhase

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0' # Or '10.0' if using a preview, though C# 10 uses .NET 6

    - name: Publish App
      # We build a 'Self-Contained' binary so you don't have to install .NET on Oracle
      # This saves RAM on the server. Change linux-x64 to linux-arm64 if using an ARM instance.
      run: |
        dotnet publish TFTStats.Presentation/TFTStats.Presentation.csproj \
        -c Release \
        -r linux-x64 \
        --self-contained true \
        -o ./publish

    - name: Copy Binary to Oracle via SSH
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.REMOTE_HOST }}
        username: ${{ secrets.REMOTE_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        source: "./publish/*"
        target: "/home/${{ secrets.REMOTE_USER }}/tftCrawler"
        strip_components: 1

    - name: Restart Crawler Service
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.REMOTE_HOST }}
        username: ${{ secrets.REMOTE_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          # Make the binary executable
          chmod +x /home/${{ secrets.REMOTE_USER }}/tftCrawler/TFTStats.Presentation
          # Reload and restart the background service
          sudo systemctl daemon-reload
          sudo systemctl restart tftCrawler
          sudo systemctl status tftCrawler --no-pager
